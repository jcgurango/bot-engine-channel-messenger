export interface IBotOptions {
    /**
     * A page token.
     */
    token: String,
    /**
     * The verify token to use.
     */
    verify?: String,
    /**
     * The FB app secret.
     */
    appSecret?: String,
}
